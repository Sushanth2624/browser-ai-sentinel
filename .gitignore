# Go
agent/bin/
agent/**/*.exe

# Python
ai-engine/.venv/
ai-engine/__pycache__/
ai-engine/**/__pycache__/
ai-engine/*.egg-info/

# Node / extension / dashboard
node_modules/
extension/dist/
extension/*.pem
dashboard/dist/
dashboard/.vite/

# secrets / env
*.env
secrets.env
.env.local
db/pg_password.txt

# data
db/pgdata/
data/

# OS / editor
.DS_Store
*.log
extension/extension_key_pub.b64
sensor/logs/

# Phase 3: generated dataset pages (deterministic — regenerate with `make dataset-gen`) and
# self-signed mock-AI certs. manifest.json and eval/results/*.json ARE tracked — ground truth
# and the actual evaluation evidence are worth keeping in the repo.
eval/dataset/*.html
sensor/mock-ai/certs/
sensor/mock-ai/*.log

# Report build venv — the generated .docx/.pdf ARE tracked (they're the deliverable/evidence,
# same pattern as capstone-1's committed report), only the venv itself is excluded.
docs/report/.venv/
